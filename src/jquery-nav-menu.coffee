(($) ->
  $.fn.navMenu = (opts)->

    class NavMenu
      constructor: (w, opts)->
        @window = w
        @begin()
        @

      begin: ->
        # start it up
        return

      destroy: ->
        # detach all event handlers
        # reset the dom to normal
        return

    new NavMenu(@, opts)
) jQuery